services:
  - type: web
    name: flask-app
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python project/main.py"
    autoDeploy: true
